{
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:gh": "vite build --mode gh",
    "deploy": "npm run build:gh && gh-pages -d dist -b gh-pages"
  }
}
